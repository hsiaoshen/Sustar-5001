<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title></title>
    <script src="https://cdn.bootcss.com/jquery/3.0.0/jquery.min.js"></script>
    <style media="screen">
        * {
            padding: 0;
            margin: 0;
        }

        .head {
            height: 100px;
            margin-top: 150px;
            margin-bottom: 50px;
        }

        .head img {
            height: 100px;
            width: 300px;
        }

        .img {
            height: 100px;
            width: 440px;
            margin: auto;
        }

        input {
            width: 300px;
            height: 30px;
            padding-left: 15px;
            border-radius: 7px;
            outline: none;
            outline-style: none;
            border: 1px solid #ccc;
        }

        span {
            color: #333333;
            font-size: 20px;
        }

        form button {
            width: 90px;
            height: 32px;
            border-radius: 5px;
            margin-left: 10px;
            outline: none;
            background: #3399CC;
            border: 1px solid #009A61;
            color: white;
        }

        .kuang {
            width: 440px;
            height: 100px;
            margin: auto;
            margin-top: 30px;
        }

        .kuang span {
            font-size: 13px;
            color: red;
        }
    </style>
</head>

<body>
    <div class="head">
        <div class="img">
            <a href="/"><img src="/images/sustar-logo.png" alt=""></a>

            <div class="kuang">
                <form class="" action="/findsussess" method="post">
                    <strong style="color: #777;">请输入新密码:</strong><br><br>
                    <input type="password" name="password" placeholder="请输入不少于6位数的密码"><button type="submit" name="button">确认密码</button>
            </form>
          </div>
        </div>
    </div>
    <script type="text/javascript">
        $('input').focus(function() {
            $(this).css({
                "box-shadow": "0px 0px 3px 1px #3388FF",
                "border": "1px solid #3388FF"
            });
        })
        $('input').blur(function() {
            $(this).css({
                "box-shadow": "0px 0px 0px 0px",
                "border": "1px solid #ccc"
            })
            $(".kuang span").css("display", "none")
        })
        $('.kuang input').blur(function() {
            var b = $(this).val();
            var c = b.length;
            if (c < 6) {
                $(".kuang").append("<span>请输入正确的密码!</span>");
                $(".try").attr("disabled", "disabled")
            } else {
                $(".try").removeAttr("disabled")
            }
        })
        $("form").submit(function() {
            var b = $("form input").val();
            if (b == "") {
                return false;
            }
        })
    </script>
</body>

</html>
